soma x y = x + y
dobro x = 2 * x

triplo :: Int -> Int
triplo x = x * 3

quadrado x = x * x

ehPar x = mod x 2 == 0

metade :: Double -> Double
metade x = x / 2

maiorDeIdade :: Int -> Bool
maiorDeIdade idade = idade >= 16